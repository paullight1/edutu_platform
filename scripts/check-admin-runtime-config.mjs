// Deliberately incomplete RED-phase scaffold. The tests define the policy;
// the next commit implements repository inspection and the CLI.
export function inspectAdminRuntimeConfigSources() {
  return [];
}
